// The Twitter/X image reuses the default Open Graph image (same rendering),
// so that the `summary_large_image` card always has a visual.
export { alt, size, contentType } from "./opengraph-image";
export { default } from "./opengraph-image";
